server:
  port: 8090

spring:
  application:
    name: Chat App

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  datasource:
    url: jdbc:postgresql://localhost:5432/chat-app
    username: chat-app-dbadmin
    password: chat-app-dummy-password

jwt:
  secret: kKTDPrQuzfFaamgYGAUeZZEDMgtdGRvV
  expiration: 3600000
